<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".presentation.home.HomeFragment"
>
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/homeFrag_body"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/homeFrag_nav"
        app:navGraph="@navigation/home_nav"
        app:defaultNavHost="true"
        android:name="androidx.navigation.fragment.NavHostFragment"
    />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/homeFrag_nav"
        android:layout_width="0dp"
        android:layout_height="56dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/bottom_nav"
        app:labelVisibilityMode="labeled"
        app:itemIconTint="@drawable/nav_item_selector"
        app:itemTextColor="@drawable/nav_item_selector"
        app:itemActiveIndicatorStyle="@color/white"
        app:itemRippleColor="@null"
        app:itemIconSize="24dp"
        app:itemPaddingTop="8dp"
        app:itemPaddingBottom="8dp"
        app:itemTextAppearanceActive="@style/BottomNavigationViewSelectedText"
        app:itemTextAppearanceInactive="@style/BottomNavigationViewUnselectedText"
    />

    <!--
        app:itemRippleColor="@null" -> off Splash color
    -->

</androidx.constraintlayout.widget.ConstraintLayout>